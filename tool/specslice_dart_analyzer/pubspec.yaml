name: specslice_dart_analyzer
description: SpecSlice Dart resolved-AST sidecar — produces a LanguageIndexBatch JSON from `package:analyzer` for use by the Rust engine.
publish_to: none
version: 0.2.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  analyzer: ^7.0.0
  args: ^2.4.0
  path: ^1.9.0
  crypto: ^3.0.3

dev_dependencies:
  test: ^1.25.0

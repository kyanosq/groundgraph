{
  "_about": "Agent code-lookup benchmark: grep arm vs specslice arm, same model/workspace/prompt. Ground truth is INDEPENDENT of specslice (hand-verified from source + ripgrep), never derived from specslice's own graph, to avoid circular evaluation.",
  "defaults": {
    "workspace": "/Users/qjs/Code/Projects/specslice-bench/wt",
    "model": "composer-2.5",
    "timeout_secs": 600
  },
  "arms": {
    "grep": "Constraints for THIS task: use ONLY ripgrep (`rg`) / `grep` via the shell plus reading files. You MUST NOT run the `specslice` command or use any specslice tool.",
    "specslice": "A `specslice` CLI is available on PATH (subcommands: search, trace, impact, dead-code, facts, graph). It is a code-graph replacement for grep. Prefer `specslice` for code navigation; you may also read files. Avoid plain grep/rg where specslice can answer it."
  },
  "tasks": [
    {
      "id": "t1_def_kill_tree",
      "bucket": "definitional (grep-fair baseline)",
      "prompt": "In this Rust workspace, locate the function named `kill_tree`. Reply on a single line in EXACTLY this format: `SIG=<the full fn signature line without the trailing brace> | LOC=<relative/path.rs>:<line>`",
      "grade": {
        "type": "fields",
        "checks": [
          { "name": "signature", "must_contain_all": ["fn kill_tree", "&mut Child"] },
          { "name": "location", "must_contain_all": ["proc.rs", "52"] }
        ]
      }
    },
    {
      "id": "t2_callcount_kill_tree",
      "bucket": "call-site precision (specslice-favored)",
      "prompt": "How many times is the function `kill_tree` actually INVOKED (called) in this workspace? Exclude its own definition, doc comments, string literals, and test code. Reply on a single line in EXACTLY this format: `CALLS=<integer> | SITES=<relative/path.rs>:<line>[,<relative/path.rs>:<line>...]`",
      "grade": {
        "type": "fields",
        "checks": [
          { "name": "call_count", "must_contain_all": ["CALLS=1"] },
          { "name": "call_site", "must_contain_all": ["proc.rs", "91"] }
        ]
      }
    },
    {
      "id": "t3_callers_detach",
      "bucket": "relational callers (cross-file set)",
      "prompt": "List every source FILE that contains a call to `detach_process_group`, EXCLUDING the file where the function is defined. Reply on a single line in EXACTLY this format using bare file basenames: `FILES=<name1.rs>,<name2.rs>,...`",
      "grade": {
        "type": "set",
        "key": "FILES",
        "truth": ["dart_sidecar.rs", "lsp_client.rs", "scip_runner.rs", "lsp_probe.rs"]
      }
    },
    {
      "id": "t4_trace_kill_and_reap",
      "bucket": "transitive call closure (grep needs many hops)",
      "prompt": "Compute the COMPLETE downstream call closure of the function `kill_and_reap`: every first-party function it calls, directly or transitively, within this workspace. Exclude standard-library / external-crate calls and test code. Reply on a single line in EXACTLY this format using bare function names: `FUNCS=<name1>,<name2>,...`",
      "grade": {
        "type": "set",
        "key": "FUNCS",
        "truth": ["kill_tree", "reap_within"]
      }
    },
    {
      "id": "t5_deadcode_island",
      "bucket": "whole-graph reachability / dead island (grep's textual ceiling)",
      "_oracle": "Rust compiler `never used` lint flags BOTH bench_dead_* (injected fixture); independent of specslice. grep is fooled because bench_dead_alpha IS textually referenced (by the equally-dead bench_dead_beta).",
      "prompt": "Find ALL dead functions in this workspace whose names start with `bench_dead_`. A function is dead if it is unreachable from any program entry point. Crucially, include a function even if other code references it, SO LONG AS that referencing code is itself dead (a transitively-dead 'island'). Reply on a single line in EXACTLY this format using bare function names: `DEAD=<name1>,<name2>,...`",
      "grade": {
        "type": "set",
        "key": "DEAD",
        "truth": ["bench_dead_alpha", "bench_dead_beta"]
      }
    }
  ]
}

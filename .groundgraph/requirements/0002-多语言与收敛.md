# REQ-P16-PYTHON Python 适配

Python 结构由 tree-sitter 产出；pytest / 装饰器框架语义与 src-root import
解析内聚到 `PYTHON_SPEC`；LSP 仅作 Tier-3 叠加 Calls/References。

## 文档
- docs/implementation-plan.md

## 实现
- crates/groundgraph-engine/src/python_indexer.rs#index_python
- crates/groundgraph-engine/src/python_treesitter.rs

## 测试
- crates/groundgraph-engine/tests/p22_treesitter_multilang.rs
- crates/groundgraph-engine/tests/lsp_indexers.rs

# REQ-P21-RUST Rust tree-sitter 广度后端与自举

新增 Rust tree-sitter 后端并用 GroundGraph 索引自身仓库（dogfood 自举），
作为通用驱动的参考实现与性能基线。

## 文档
- docs/implementation-plan.md

## 实现
- crates/groundgraph-engine/src/rust_indexer.rs#index_rust
- crates/groundgraph-engine/src/rust_treesitter.rs#scan

## 测试
- crates/groundgraph-engine/tests/p21_rust_self_host.rs

# REQ-P22-TREESITTER 通用 tree-sitter 广度后端

以数据驱动的 `LangSpec` 统一多语言结构抽取（符号、Contains、symbol_ranges、
import 解析、测试节点、框架 metadata），成为唯一结构来源。

## 文档
- docs/implementation-plan.md

## 实现
- crates/groundgraph-engine/src/treesitter.rs#index_repo_with_spec

## 测试
- crates/groundgraph-engine/tests/p22_treesitter_multilang.rs
- crates/groundgraph-engine/tests/p23_totality_proptest.rs

# REQ-P23-CONFIG 统一配置与单一索引通路

引入统一 `languages:` + `enrichment:` 配置（旧 per-language 键作弃用别名），
`normalized()` 折叠为唯一权威来源；`index_repository` 合为单一结构 pass。

## 文档
- docs/implementation-plan.md

## 实现
- crates/groundgraph-engine/src/config.rs#normalized
- crates/groundgraph-engine/src/index.rs#index_repository

## 测试
- crates/groundgraph-engine/tests/config_schema.rs
- crates/groundgraph-engine/tests/p23_sqlite_grade.rs

# REQ-P23-REQMD Markdown 需求映射格式

`.groundgraph/requirements/*.md` 解析为 `Requirement` 节点与
`Documents`/`DeclaresImplementation`/`DeclaresVerification` 边，作为
links.yaml 的人性化继任格式（本文件即由该格式编写）。

## 文档
- docs/implementation-plan.md

## 实现
- crates/groundgraph-engine/src/requirements_md_indexer.rs#index_requirements_md

## 测试
- crates/groundgraph-engine/tests/requirements_md.rs
